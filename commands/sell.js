const { MessageEmbed } = require("discord.js");

exports.execute = async (client, message, args) => {
  
  let sellItem = client.shop[x.toLowerCase()];
  if (!sellItem || sellItem == undefined)
    return message.reply("ийм бараа байхгүй байна");
};
