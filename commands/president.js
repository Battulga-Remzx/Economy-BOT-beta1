const { MessageEmbed } = require("discord.js")

exports.execute = async (client, message, args) => {
let author = message.author
let amount = Math.floor(Math.random() * 10000-9999) + 1;
  let pre = client.eco.pre(client.ecoAddUser, amoint)
}